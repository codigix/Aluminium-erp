const mysql = require('mysql2/promise'); async function run() { try { const connection = await mysql.createConnection({ host: '127.0.0.1', user: 'aluminium_user', password: 'C0digix$309', database: 'sales_erp', port: 3307 }); const [cols] = await connection.query('DESCRIBE sales_order_item_materials'); console.log(JSON.stringify(cols, null, 2)); await connection.end(); } catch (e) { console.error(e); } process.exit(0); } run();  
