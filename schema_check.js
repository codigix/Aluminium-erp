const mysql = require('mysql2/promise'); require('dotenv').config({ path: './backend/.env' }); async function run() { const pool = mysql.createPool({ host: process.env.DB_HOST, user: process.env.DB_USER, password: process.env.DB_PASSWORD, database: process.env.DB_NAME }); const [rows] = await pool.query(\"DESCRIBE quotation_requests\"); console.log(JSON.stringify(rows)); process.exit(0); } run().catch(err = console.error(err); process.exit(1); })  
