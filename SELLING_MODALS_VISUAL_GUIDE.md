# 📊 Selling Module Modal Forms - Visual Guide

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    SELLING MODULE - MODALS                       │
└─────────────────────────────────────────────────────────────────┘

                    ┌─────────────────────────────────┐
                    │   SELLING DASHBOARD PAGES       │
                    └─────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┬───────────────┐
        │                   │                   │               │
        ▼                   ▼                   ▼               ▼
    Quotations          Sales Orders      Delivery Notes   Invoices     Customers
        │                   │                   │               │          │
        │                   │                   │               │          │
        ▼                   ▼                   ▼               ▼          ▼
   [+] New Button     [+] New Button      [+] New Button [+] New Button [+] New Button
        │                   │                   │               │          │
        └───────────────────┼───────────────────┼───────────────┼──────────┘
                            │
        ┌───────────────────┼───────────────────┬───────────────┬──────────┐
        │                   │                   │               │          │
        ▼                   ▼                   ▼               ▼          ▼
    Quotation Modal  Sales Order Modal  Delivery Modal  Invoice Modal Customer Modal
        │                   │                   │               │          │
        └───────────────────┴───────────────────┴───────────────┴──────────┘
                            │
                    ┌───────▼──────────┐
                    │  Modal Component │
                    │  (Shared Wrapper)│
                    └───────┬──────────┘
                            │
        ┌───────────────────┼───────────────────┬────────────────┐
        │                   │                   │                │
        ▼                   ▼                   ▼                ▼
    Forms              Validation          Loading States    Error Display
    └─────────────┬─────────────┬─────────────┬──────────────────┘
                  │             │             │
                  ▼             ▼             ▼
            API Calls      Success Callback   Close Modal
                  │             │             │
                  └─────────────┼─────────────┘
                                ▼
                        List Auto-Refresh
```

---

## Modal Lifecycle

```
MODAL LIFECYCLE
═══════════════════════════════════════════════════════════════

1. USER CLICKS "NEW" BUTTON
   │
   ├─► setState(showModal = true)
   │
   └─► Trigger useEffect → Fetch data (customers, orders, etc.)

2. MODAL OPENS
   │
   ├─► Fade-in overlay (300ms)
   │
   ├─► Slide-up modal (300ms)
   │
   └─► Display form with empty fields

3. USER FILLS FORM
   │
   ├─► Input changes update formData state
   │
   ├─► Clear error messages on change
   │
   └─► Show required field indicators (*)

4. USER SUBMITS
   │
   ├─► Validate required fields
   │
   ├─► Set loading = true (disable button)
   │
   ├─► POST to API endpoint
   │
   └─► Handle response

5. SUCCESS
   │
   ├─► Reset formData state
   │
   ├─► Call onSuccess callback
   │
   ├─► Close modal
   │
   └─► List auto-refreshes

6. ERROR
   │
   ├─► Set loading = false
   │
   ├─► Display error message
   │
   └─► Keep modal open for retry

7. USER CLOSES
   │
   ├─► Click X button → Modal closes
   │
   ├─► Click Cancel → Modal closes
   │
   └─► Click overlay → Modal closes
```

---

## Modal Component Sizes

```
┌─────────────────────────────────────────────────────────────┐
│                    MODAL SIZING                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  DESKTOP VIEW (>768px)                                      │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  📋 Create New Quotation                         [X]  │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │                                                        │  │
│  │  Customer *           │  Amount (₹) *                │  │
│  │  [Select Customer▼]   │  [0.00           ]           │  │
│  │                                                        │  │
│  │  Valid Till *         │  Notes                       │  │
│  │  [2024-12-31]         │  [Reference text...]         │  │
│  │                                                        │  │
│  │  ┌────────────────────────────────────────────────┐   │  │
│  │  │ [Cancel]                   [✓ Create Quotation]│   │  │
│  │  └────────────────────────────────────────────────┘   │  │
│  │                                                        │  │
│  └───────────────────────────────────────────────────────┘  │
│                    Width: 900px (lg)                         │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  TABLET VIEW (480-768px)                                    │
│  ┌──────────────────────────────┐                           │
│  │ 📋 Create New Quotation [X] │                           │
│  ├──────────────────────────────┤                           │
│  │                              │                           │
│  │ Customer * [Select C...▼]    │                           │
│  │ Amount (₹) * [0.00      ]    │                           │
│  │ Valid Till * [2024-12-31]    │                           │
│  │ Notes [Reference text...]    │                           │
│  │                              │                           │
│  │ [Cancel] [✓ Create Quotation]│                           │
│  │                              │                           │
│  └──────────────────────────────┘                           │
│          Width: 95% (560px)                                 │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  MOBILE VIEW (<480px)                                       │
│  ┌──────────────────────┐                                   │
│  │ 📋 Create Quote [X]  │                                   │
│  ├──────────────────────┤                                   │
│  │ Customer *           │                                   │
│  │ [Select...▼]         │                                   │
│  │                      │                                   │
│  │ Amount (₹) *         │                                   │
│  │ [0.00            ]   │                                   │
│  │                      │                                   │
│  │ Valid Till *         │                                   │
│  │ [2024-12-31]         │                                   │
│  │                      │                                   │
│  │ Notes                │                                   │
│  │ [Reference text...]  │                                   │
│  │                      │                                   │
│  │ [Cancel]             │                                   │
│  │ [✓ Create Quotation] │                                   │
│  │                      │                                   │
│  └──────────────────────┘                                   │
│   Width: 100% - 20px padding                                │
│   Height: ~600px (scrollable)                               │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Animation Timeline

```
MODAL OPEN ANIMATION
═══════════════════════════════════════════════════════════════

Time (ms)     0          100        200        300
              │          │          │          │
Overlay       ├─ 0% ────► 50% ────► 100% ─────┤ Complete
Opacity       │                                │
              └────────────────────────────────┘

Modal         ├─ Bottom ─► Moving up ──► Position ─┤ Complete
Position      │           (-50px)        (0px)      │
              └────────────────────────────────────┘

Easing: ease-in-out


MODAL CLOSE ANIMATION
═══════════════════════════════════════════════════════════════

Time (ms)     0          100        200
              │          │          │
Overlay       ├─ 100% ─► 50% ────► 0% Complete
Opacity       │                    │
              └────────────────────┘

Modal         ├─ Position (0px) ► Moving down ► Complete
Position      │                   (50px down)
              └────────────────────────────────┘

Easing: ease-in-out


BUTTON HOVER ANIMATION
═══════════════════════════════════════════════════════════════

Time (ms)     0          100        150
              │          │          │
Transform     ├─ scale(1) ─► scale(1.02) Complete
              │
Shadow        ├─ Normal ────► Enhanced Complete
              │
Color         ├─ Base ───────► Lighter Complete
              │
              └─ Duration: 150ms, easing: ease-out
```

---

## Form Field Types

```
QUOTATION FORM
═════════════════════════════════════════════

┌─────────────────────────────────────────┐
│ Customer *                              │
├─────────────────────────────────────────┤
│ [Select Customer                      ▼ │  ← Dropdown
│  ├─ ABC Corporation                     │     (auto-fetched)
│  ├─ XYZ Industries                      │
│  └─ PQR Enterprises                     │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Quotation Amount (₹) *                  │
├─────────────────────────────────────────┤
│ [50000.00                             ] │  ← Number input
│  Valid range: 0 to infinity             │     (step 0.01)
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Valid Till *                            │
├─────────────────────────────────────────┤
│ [2024-12-31                           ] │  ← Date picker
│  Pick from calendar                     │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Notes                                   │
├─────────────────────────────────────────┤
│ [Reference text for quote..........  ] │  ← Text input
└─────────────────────────────────────────┘


CUSTOMER FORM (Complex Example)
═════════════════════════════════════════════

┌──────────────────────┬──────────────────────┐
│ Customer Name *      │ Email *              │
├──────────────────────┼──────────────────────┤
│ [ABC Corporation   ] │ [abc@company.com   ] │  ← Text inputs
└──────────────────────┴──────────────────────┘

┌──────────────────────┬──────────────────────┐
│ Phone *              │ GST Number           │
├──────────────────────┼──────────────────────┤
│ [+91-9876543210    ] │ [22ABCDE1234F1Z5   ] │  ← Phone/Text
└──────────────────────┴──────────────────────┘

┌──────────────────────┬──────────────────────┐
│ Credit Limit (₹)     │ Status               │
├──────────────────────┼──────────────────────┤
│ [100000.00         ] │ [Active            ▼] │  ← Number/Dropdown
└──────────────────────┴──────────────────────┘

┌─────────────────────────────────────────────┐
│ Billing Address                             │
├─────────────────────────────────────────────┤
│ [123 Main St, Delhi, 110001            ] │  ← TextArea
│ [Line 2...]                              │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Shipping Address                            │
├─────────────────────────────────────────────┤
│ [456 Branch St, Mumbai, 400001         ] │  ← TextArea
│ [Line 2...]                              │
└─────────────────────────────────────────────┘
```

---

## Error Handling Display

```
ERROR SCENARIOS
═══════════════════════════════════════════════════════════════

1. VALIDATION ERROR
   ┌─────────────────────────────────────────────────────────┐
   │ ⚠ Please fill in all required fields                    │
   └─────────────────────────────────────────────────────────┘

2. API ERROR
   ┌─────────────────────────────────────────────────────────┐
   │ ⚠ Failed to create quotation                            │
   └─────────────────────────────────────────────────────────┘

3. NETWORK ERROR
   ┌─────────────────────────────────────────────────────────┐
   │ ⚠ Network error. Please check your connection.          │
   └─────────────────────────────────────────────────────────┘

4. FIELD-LEVEL ERROR
   ┌─────────────────────────────────┐
   │ Email *                         │
   ├─────────────────────────────────┤
   │ [invalid-email@invalid        ] │
   │ ⚠ Invalid email format          │ ← Red text
   └─────────────────────────────────┘

Style:
├─ Background: #fee2e2 (light red)
├─ Border: 1px solid #fecaca
├─ Color: #dc2626 (dark red)
├─ Icon: AlertCircle (18px)
└─ Margin: 20px bottom
```

---

## Loading States

```
LOADING STATE
═══════════════════════════════════════════════════════════════

Normal State:
┌─────────────────────────────────┐
│ [✓ Create Quotation]            │  ← Clickable, blue gradient
└─────────────────────────────────┘

Loading State:
┌─────────────────────────────────┐
│ [⟳ Creating...] (disabled)      │  ← Loading spinner, disabled
└─────────────────────────────────┘
     Opacity: 0.65
     Cursor: not-allowed
     Pointer-events: none

Spinner Animation:
     ⟳ → ⟲ → ⟳ (continuous rotation)
     Duration: 1s per rotation
     Repeat: infinite
```

---

## Dropdown Population

```
DROPDOWN AUTO-FETCH & POPULATE
═══════════════════════════════════════════════════════════════

QUOTATION MODAL
│
└─► useEffect → Fetch customers API
    │
    ├─► GET /api/selling/customers
    │
    └─► setState(customers = [
            { id: 1, name: "ABC Corp" },
            { id: 2, name: "XYZ Inc" },
            { id: 3, name: "PQR Ltd" }
        ])
        │
        └─► Render dropdown:
            [Select Customer        ▼]
            ├─ ABC Corp
            ├─ XYZ Inc
            └─ PQR Ltd


DELIVERY NOTE MODAL
│
└─► useEffect → Fetch sales orders (confirmed only)
    │
    ├─► GET /api/selling/sales-orders?status=confirmed
    │
    └─► setState(orders = [
            { id: 1, order_id: "SO-001", customer: "ABC" },
            { id: 2, order_id: "SO-002", customer: "XYZ" }
        ])
        │
        └─► Render dropdown:
            [Select Sales Order     ▼]
            ├─ SO-001 - ABC Corp
            └─ SO-002 - XYZ Inc


INVOICE MODAL
│
└─► useEffect → Fetch delivery notes (delivered only)
    │
    ├─► GET /api/selling/delivery-notes?status=delivered
    │
    └─► setState(deliveryNotes = [
            { id: 1, delivery_id: "DN-001", customer: "ABC" }
        ])
        │
        └─► Render dropdown:
            [Select Delivery Note  ▼]
            └─ DN-001 - ABC Corp
```

---

## State Management Pattern

```
MODAL STATE STRUCTURE
═══════════════════════════════════════════════════════════════

const [loading, setLoading] = useState(false)
const [error, setError] = useState(null)
const [formData, setFormData] = useState({
  customer_id: '',
  customer_name: '',
  total_value: '',
  valid_till: '',
  notes: ''
})

FORM DATA UPDATES:
├─ On Input Change:
│  └─ setFormData(prev => ({ ...prev, [name]: value }))
│     └─ setError(null)  ← Clear error on change
│
├─ On Submit Start:
│  └─ setLoading(true)
│
├─ On Submit Success:
│  ├─ Reset formData
│  ├─ Call onSuccess()
│  ├─ setShowModal(false)
│  └─ setLoading(false)
│
└─ On Submit Error:
   ├─ setError(error.message)
   └─ setLoading(false)
```

---

## API Integration Pattern

```
FORM SUBMISSION FLOW
═══════════════════════════════════════════════════════════════

const handleSubmit = async (e) => {
    e.preventDefault()
    setLoading(true)
    setError(null)

    try {
        // 1. VALIDATION
        if (!formData.customer_id || !formData.total_value) {
            throw new Error('Please fill in required fields')
        }

        // 2. API CALL
        const response = await fetch(
            'http://localhost:5000/api/selling/quotations',
            {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    customer_id: formData.customer_id,
                    customer_name: formData.customer_name,
                    total_value: parseFloat(formData.total_value),
                    valid_till: formData.valid_till,
                    notes: formData.notes,
                    status: 'draft'
                })
            }
        )

        const data = await response.json()

        // 3. ERROR HANDLING
        if (!response.ok) {
            throw new Error(data.error || 'Failed to create quotation')
        }

        // 4. SUCCESS
        setFormData({ /* reset */ })
        onSuccess?.()
        onClose()

    } catch (err) {
        setError(err.message)

    } finally {
        setLoading(false)
    }
}
```

---

## Page Integration Pattern

```
INTEGRATING MODAL INTO PAGE
═══════════════════════════════════════════════════════════════

// 1. IMPORT
import CreateQuotationModal from '../../components/Selling/CreateQuotationModal'

// 2. STATE
const [showModal, setShowModal] = useState(false)

// 3. BUTTON
<Button onClick={() => setShowModal(true)}>
    <Plus size={18} /> New Quotation
</Button>

// 4. COMPONENT
<CreateQuotationModal 
    isOpen={showModal}
    onClose={() => setShowModal(false)}
    onSuccess={fetchQuotations}  ← Re-fetch data
/>

// RESULT
├─ Button click opens modal
├─ Form submission calls API
├─ Success triggers fetchQuotations()
├─ List auto-refreshes
└─ Modal auto-closes
```

---

## Performance Optimization

```
OPTIMIZATION TECHNIQUES
═══════════════════════════════════════════════════════════════

1. CONDITIONAL RENDERING
   └─ Modal only renders when isOpen = true
      └─ return null when closed → No DOM updates

2. LAZY LOADING
   └─ Customers/Orders fetched only when modal opens
      └─ useEffect([isOpen]) → Only on open

3. DEBOUNCED INPUT
   └─ Form submission has loading state
      └─ Button disabled during API call
      └─ Prevents double submission

4. OPTIMIZED ANIMATIONS
   └─ Using transform + opacity (GPU accelerated)
      └─ Not animating width/height (causes reflow)
      └─ Result: 60fps animations

5. MEMO'D COMPONENTS
   └─ Could use React.memo() to prevent re-renders
      └─ Not needed for modals (infrequent renders)

RESULT: <1000ms total interaction time
```

---

## Complete Workflow Diagram

```
USER JOURNEY: QUOTATION CREATION
═══════════════════════════════════════════════════════════════

START
  │
  ├─► Navigate to Quotations page
  │   └─► List of existing quotations displays
  │
  ├─► Click "[+] New Quotation" button
  │   └─► showModal state = true
  │
  ├─► Modal opens (animation)
  │   ├─► Overlay fades in (300ms)
  │   ├─► Modal slides up (300ms)
  │   └─► Customers dropdown auto-fetches
  │
  ├─► User fills form
  │   ├─► Select customer from dropdown
  │   ├─► Enter quotation amount
  │   ├─► Pick valid till date
  │   └─► Add optional notes
  │
  ├─► Click "[✓ Create Quotation]" button
  │   ├─► Validate required fields
  │   ├─► If error → Show error message & stop
  │   └─► If valid → Continue
  │
  ├─► Submit to API
  │   ├─► Button shows loading state
  │   ├─► POST /api/selling/quotations
  │   ├─► Wait for response...
  │   └─► If error → Show error & allow retry
  │
  ├─► Success!
  │   ├─► Reset form data
  │   ├─► Call onSuccess callback
  │   ├─► Close modal (animation)
  │   └─► Trigger fetchQuotations()
  │
  ├─► List auto-refreshes
  │   ├─► GET /api/selling/quotations
  │   ├─► New quotation appears at top
  │   └─► No page reload!
  │
  └─► END (User sees new quotation in list)
```

---

**Visual Guide Complete!** 🎉

Use this guide to understand the modal system architecture and workflows.