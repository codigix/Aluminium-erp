const mysql = require('mysql2/promise'); require('dotenv').config({ path: './.env' }); async function run() { const pool = mysql.createPool({ host: process.env.DB_HOST || '127.0.0.1', user: process.env.DB_USER || 'root', password: process.env.DB_PASSWORD || '', database: 'spTech_dev', port: process.env.DB_PORT || 3306 }); const id = '49deaab5-9d7a-4112-b087-15f714643858'; const whereClause = 'so.public_id = ?'; const [rows] = await pool.query(\SELECT so.id, c.company_name, COALESCE(ct.email, cd_client.email, '') as email_address, COALESCE(ct.phone, cd_client.phone, '') as contact_phone, COALESCE(ct.name, cd_client.contact_person, '') as contact_person, COALESCE(ba.billing_address, cd_client.billing_address, '') as billing_address, COALESCE(sa.shipping_address, cd_client.shipping_address, '') as shipping_address FROM sales_orders so LEFT JOIN companies c ON c.id = so.company_id LEFT JOIN (SELECT company_id, email, phone, name, ROW_NUMBER() OVER (PARTITION BY company_id ORDER BY contact_type = 'PRIMARY' DESC, id ASC) as rn FROM contacts) ct ON ct.company_id = c.id AND ct.rn = 1 LEFT JOIN (SELECT company_id, CONCAT_WS(', ', NULLIF(line1, ''), NULLIF(line2, ''), NULLIF(city, ''), NULLIF(state, ''), NULLIF(pincode, ''), NULLIF(country, '')) as billing_address FROM company_addresses WHERE address_type = 'BILLING' GROUP BY company_id) ba ON ba.company_id = c.id LEFT JOIN (SELECT company_id, CONCAT_WS(', ', NULLIF(line1, ''), NULLIF(line2, ''), NULLIF(city, ''), NULLIF(state, ''), NULLIF(pincode, ''), NULLIF(country, '')) as shipping_address FROM company_addresses WHERE address_type = 'SHIPPING' GROUP BY company_id) sa ON sa.company_id = c.id LEFT JOIN (SELECT client_name, MAX(billing_address) as billing_address, MAX(shipping_address) as shipping_address, MAX(contact_person) as contact_person, MAX(email) as email, MAX(phone) as phone FROM customer_drawings GROUP BY client_name) cd_client ON cd_client.client_name = c.company_name WHERE \\, [id]); console.log(JSON.stringify(rows[0], null, 2)); pool.end(); } run();
